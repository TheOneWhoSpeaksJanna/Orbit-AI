name: ARM64 Pipeline Test

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  arm64-test:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Install system tools
        run: |
          sudo apt-get update
          sudo apt-get install -y android-sdk-libsparse-utils e2fsprogs wget unzip file

      - name: Verify ARM64 architecture
        run: |
          echo "=== Architecture ==="
          uname -m
          echo "=== ptrace test ==="
          python3 -c "
          import os, ctypes
          pid = os.fork()
          if pid == 0:
              import time; time.sleep(2); os._exit(0)
          else:
              libc = ctypes.CDLL('libc.so.6', use_errno=True)
              r = libc.ptrace(16, pid, 0, 0)
              print('ptrace: ' + ('ALLOWED' if r == 0 else 'BLOCKED'))
              os.waitpid(pid, 0)
          "

      # ── Step 1: Get Android Bionic libraries ──────────────────────
      # The Termux binaries are ARM64 ELF files that need /system/bin/linker64
      # (Android's Bionic dynamic linker) and /system/lib64/libc.so.
      # These only exist on Android devices or in Android system images.
      # We download the AOSP ATD system image and extract the real Bionic libs.
      - name: Download Android system image
        run: |
          echo "Downloading AOSP ATD ARM64 system image (~300MB)..."
          wget -q "https://dl.google.com/android/repository/sys-img/aosp_atd/arm64-v8a-35_r01.zip" -O system-image.zip
          unzip -q system-image.zip
          ls -la arm64-v8a/system.img

      - name: Extract Bionic libraries from system image
        run: |
          set +e  # Don't exit on error — we want to try all strategies

          # Convert sparse image to raw ext4
          simg2img arm64-v8a/system.img system.raw 2>/dev/null || cp arm64-v8a/system.img system.raw
          echo "Raw image size: $(du -h system.raw | cut -f1)"

          sudo mkdir -p /system/bin /system/lib64

          # First, let's explore the filesystem to find Bionic libs
          echo "=== Root directory listing ==="
          debugfs -R "ls" system.raw 2>/dev/null

          echo "=== /system directory listing ==="
          debugfs -R "ls system" system.raw 2>/dev/null

          echo "=== /system/lib64 directory listing ==="
          debugfs -R "ls system/lib64" system.raw 2>/dev/null

          echo "=== /system/lib64/bootstrap directory listing ==="
          debugfs -R "ls system/lib64/bootstrap" system.raw 2>/dev/null

          echo "=== /system/apex directory listing ==="
          debugfs -R "ls system/apex" system.raw 2>/dev/null

          # Try to find all .so files and linker64
          echo "=== Searching for Bionic libraries ==="
          debugfs -R "ncheck system/lib64/bootstrap/libc.so" system.raw 2>/dev/null
          debugfs -R "ncheck system/lib64/libc.so" system.raw 2>/dev/null
          debugfs -R "ncheck system/bin/linker64" system.raw 2>/dev/null

          # Extract Bionic libs from bootstrap directory
          for lib in libc.so libm.so libdl.so libdl_android.so; do
            echo "Trying to extract system/lib64/bootstrap/$lib..."
            debugfs -R "dump system/lib64/bootstrap/$lib /tmp/$lib" system.raw 2>/dev/null
            if [ -f "/tmp/$lib" ] && [ $(stat -c%s "/tmp/$lib") -gt 1000 ]; then
              sudo cp "/tmp/$lib" /system/lib64/
              echo "  ✓ $lib ($(stat -c%s /tmp/$lib) bytes)"
            else
              # Try without bootstrap/
              echo "  Trying system/lib64/$lib..."
              debugfs -R "dump system/lib64/$lib /tmp/$lib" system.raw 2>/dev/null
              if [ -f "/tmp/$lib" ] && [ $(stat -c%s "/tmp/$lib") -gt 1000 ]; then
                sudo cp "/tmp/$lib" /system/lib64/
                echo "  ✓ $lib from system/lib64 ($(stat -c%s /tmp/$lib) bytes)"
              else
                echo "  ✗ $lib not found anywhere"
              fi
            fi
          done

          # Extract linker64
          echo "=== Extracting linker64 ==="
          # Try system/bin/linker64 first
          debugfs -R "dump system/bin/linker64 /tmp/linker64" system.raw 2>/dev/null
          if [ -f "/tmp/linker64" ] && [ $(stat -c%s "/tmp/linker64") -gt 1000 ]; then
            sudo cp /tmp/linker64 /system/bin/linker64
            sudo chmod +x /system/bin/linker64
            echo "  ✓ linker64 ($(stat -c%s /tmp/linker64) bytes)"
          else
            # Try from APEX
            echo "  Trying APEX extraction..."
            # List apex files to find the right one
            debugfs -R "ls system/apex" system.raw 2>/dev/null | grep -i "runtime"

            # Try different APEX names
            for apex_name in com.android.runtime.apex com.android.runtime.release.apex; do
              echo "  Trying $apex_name..."
              debugfs -R "dump system/apex/$apex_name /tmp/runtime.apex" system.raw 2>/dev/null
              if [ -f /tmp/runtime.apex ] && [ $(stat -c%s /tmp/runtime.apex) -gt 100000 ]; then
                echo "  APEX found: $(stat -c%s /tmp/runtime.apex) bytes"
                cd /tmp
                rm -rf apex_dir/
                unzip -q runtime.apex -d apex_dir/ 2>/dev/null
                ls apex_dir/ 2>/dev/null

                # Check if apex_payload.img exists
                if [ -f apex_dir/apex_payload.img ]; then
                  echo "  APEX payload found"
                  # List contents
                  debugfs -R "ls bin" apex_dir/apex_payload.img 2>/dev/null
                  debugfs -R "ls lib64" apex_dir/apex_payload.img 2>/dev/null
                  debugfs -R "ls lib64/bionic" apex_dir/apex_payload.img 2>/dev/null

                  # Extract linker64
                  debugfs -R "dump bin/linker64 /tmp/linker64" apex_dir/apex_payload.img 2>/dev/null
                  if [ -f /tmp/linker64 ] && [ $(stat -c%s /tmp/linker64) -gt 1000 ]; then
                    sudo cp /tmp/linker64 /system/bin/linker64
                    sudo chmod +x /system/bin/linker64
                    echo "  ✓ linker64 from APEX ($(stat -c%s /tmp/linker64) bytes)"
                  fi

                  # Extract Bionic libs from APEX
                  for lib in libc.so libm.so libdl.so libdl_android.so liblog.so; do
                    debugfs -R "dump lib64/bionic/$lib /tmp/apex_$lib" apex_dir/apex_payload.img 2>/dev/null
                    if [ -f "/tmp/apex_$lib" ] && [ $(stat -c%s "/tmp/apex_$lib") -gt 1000 ]; then
                      sudo cp "/tmp/apex_$lib" /system/lib64/ 2>/dev/null
                      echo "  ✓ $lib from APEX"
                    fi
                  done
                fi
                cd -
                break
              fi
            done
          fi

          # Extract other system libs
          for lib in liblog.so libbase.so libc++.so libz.so libcrypto.so libssl.so; do
            debugfs -R "dump system/lib64/$lib /tmp/$lib" system.raw 2>/dev/null
            if [ -f "/tmp/$lib" ] && [ $(stat -c%s "/tmp/$lib") -gt 1000 ]; then
              sudo cp "/tmp/$lib" /system/lib64/ 2>/dev/null && echo "  ✓ $lib" || true
            fi
          done

          # Verify
          echo ""
          echo "=== Bionic setup verification ==="
          ls -la /system/bin/linker64 2>&1
          file /system/bin/linker64 2>&1
          ls -la /system/lib64/libc.so 2>&1
          file /system/lib64/libc.so 2>&1
          echo "=== All /system/lib64 contents ==="
          ls -la /system/lib64/ 2>&1

          # Don't fail the build if Bionic extraction is incomplete —
          # we want to see how far PRoot gets even without all libs
          exit 0

      # ── Step 2: Set up Termux rootfs ──────────────────────────────
      - name: Extract Termux bootstrap
        run: |
          mkdir -p rootfs/data/data/com.termux/files/usr
          unzip -q app/src/main/assets/termux-bootstrap.zip \
            -d rootfs/data/data/com.termux/files/usr/

          # Create symlinks from SYMLINKS.txt
          cd rootfs/data/data/com.termux/files/usr
          count=0
          while IFS= read -r line; do
            [ -z "$line" ] && continue
            target=$(echo "$line" | sed 's/←.*//' | sed 's/^\.\///')
            linkname=$(echo "$line" | sed 's/.*←//' | sed 's/^\.\///')
            dir=$(dirname "$linkname")
            mkdir -p "$dir"
            ln -sf "$target" "$linkname" 2>/dev/null && count=$((count+1)) || true
          done < SYMLINKS.txt
          echo "Created $count symlinks"
          cd -

          # Create necessary directories
          mkdir -p rootfs/data/data/com.termux/files/home
          mkdir -p rootfs/data/data/com.termux/files/usr/tmp
          mkdir -p rootfs/data/data/com.termux/cache/apt/archives/partial
          mkdir -p rootfs/etc
          echo "nameserver 8.8.8.8" > rootfs/etc/resolv.conf

          # apt sources.list
          mkdir -p rootfs/data/data/com.termux/files/usr/etc/apt
          echo "deb https://packages.termux.dev/apt/termux-main/ stable main" \
            > rootfs/data/data/com.termux/files/usr/etc/apt/sources.list

          # Verify
          echo "=== Rootfs verification ==="
          ls -la rootfs/data/data/com.termux/files/usr/bin/sh
          ls -la rootfs/data/data/com.termux/files/usr/bin/bash
          ls -la rootfs/data/data/com.termux/files/usr/bin/apt

      # ── Step 3: Test the full pipeline ────────────────────────────
      - name: Test 1 - Basic PRoot execution
        run: |
          chmod +x app/src/main/jniLibs/arm64-v8a/libproot.so
          PROOT="$PWD/app/src/main/jniLibs/arm64-v8a/libproot.so"
          export PROOT_LOADER="$PWD/app/src/main/jniLibs/arm64-v8a/libproot_loader.so"

          echo "=== Test: echo hello via PRoot ==="
          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c "echo 'Hello from PRoot on ARM64!'"
          echo "EXIT=$?"

      - name: Test 2 - dpkg --configure -a
        run: |
          PROOT="$PWD/app/src/main/jniLibs/arm64-v8a/libproot.so"
          export PROOT_LOADER="$PWD/app/src/main/jniLibs/arm64-v8a/libproot_loader.so"

          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c "dpkg --configure -a 2>&1 || true"
          echo "EXIT=$?"

      - name: Test 3 - apt update
        run: |
          PROOT="$PWD/app/src/main/jniLibs/arm64-v8a/libproot.so"
          export PROOT_LOADER="$PWD/app/src/main/jniLibs/arm64-v8a/libproot_loader.so"

          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c \
            "apt update -o APT::Sandbox::User=root 2>&1"
          echo "EXIT=$?"

      - name: Test 4 - apt install nodejs git python3
        run: |
          PROOT="$PWD/app/src/main/jniLibs/arm64-v8a/libproot.so"
          export PROOT_LOADER="$PWD/app/src/main/jniLibs/arm64-v8a/libproot_loader.so"

          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c \
            "apt install -y -o APT::Sandbox::User=root nodejs git python3 2>&1"
          echo "EXIT=$?"

          # Verify tools installed
          echo "=== Tool verification ==="
          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c \
            "node --version && npm --version && git --version"

      - name: Test 5 - npm install -g @gitlawb/openclaude
        run: |
          PROOT="$PWD/app/src/main/jniLibs/arm64-v8a/libproot.so"
          export PROOT_LOADER="$PWD/app/src/main/jniLibs/arm64-v8a/libproot_loader.so"

          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c \
            "npm install -g @gitlawb/openclaude 2>&1"
          echo "EXIT=$?"

      - name: Test 6 - Agent entry point resolution
        run: |
          PROOT="$PWD/app/src/main/jniLibs/arm64-v8a/libproot.so"
          export PROOT_LOADER="$PWD/app/src/main/jniLibs/arm64-v8a/libproot_loader.so"

          echo "=== Check if openclaude binary exists ==="
          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c \
            "which openclaude 2>/dev/null || echo 'NOT IN PATH'"

          echo "=== Try executing openclaude directly ==="
          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c \
            "openclaude --version 2>&1 || echo 'EXEC FAILED (shebang issue?)'"

          echo "=== Resolve symlink ==="
          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c \
            "readlink -f \$PREFIX/bin/openclaude 2>/dev/null || echo 'SYMLINK FAILED'"

          echo "=== Try node with resolved path ==="
          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c \
            "RESOLVED=\$(readlink -f \$PREFIX/bin/openclaude) && node \"\$RESOLVED\" --version 2>&1 || echo 'NODE EXEC FAILED'"

      - name: Test 7 - Agent prompt execution
        run: |
          PROOT="$PWD/app/src/main/jniLibs/arm64-v8a/libproot.so"
          export PROOT_LOADER="$PWD/app/src/main/jniLibs/arm64-v8a/libproot_loader.so"

          echo "=== Send test prompt to agent via stdin ==="
          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c \
            'echo "hi" | openclaude 2>&1; echo "EXIT=$?"'

          echo ""
          echo "=== Try node with resolved path + stdin ==="
          $PROOT --kill-on-exit \
            -r rootfs \
            -b /dev -b /proc -b /sys \
            -b /system -b /system/bin -b /system/lib64 \
            -b /data/local/tmp:/tmp \
            -w /data/data/com.termux/files/home \
            /data/data/com.termux/files/usr/bin/sh -c \
            'RESOLVED=$(readlink -f $PREFIX/bin/openclaude) && echo "hi" | node "$RESOLVED" 2>&1; echo "EXIT=$?"'

      - name: Summary
        if: always()
        run: |
          echo "========================================="
          echo "ARM64 Pipeline Test Summary"
          echo "========================================="
          echo "Architecture: $(uname -m)"
          echo ""
          echo "If any test above shows EXIT=0, that step passed."
          echo "If tests failed, check the logs above for the exact error."
          echo "The most common failure point is Test 1 (basic PRoot exec)"
          echo "which means the Bionic linker setup failed."
          echo "========================================="
